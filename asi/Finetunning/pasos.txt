Hay que ejecutar el siguiente comando para poder activar el entorno de Python

venv\Scripts\Activate.ps1 (este es el comando para powershell, el del doc de pedro es para el cdm)
 
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
  

Debes tener git instalado

Axolotl NO es compatible con Windows nativo.

vamos a usar WSL2 + Ubuntu

Estoy configurando un entorno para hacer fine-tuning con Axolotl, que no es compatible con Windows nativo, por lo que estoy usando WSL2 + Ubuntu.

Mis requerimientos de la práctica son:

Python y Git instalados

GPU NVIDIA con drivers CUDA

Uso de Axolotl para fine-tuning

Ya he hecho lo siguiente:

Instalé WSL

Reinicié el equipo

Al ejecutar wsl --status obtuve este error:

WSL2 no es compatible con la configuración actual de la máquina.
Se debe habilitar el componente opcional "Plataforma de máquina virtual"
y asegurarse de que la virtualización esté habilitada en el BIOS.


Ubuntu no aparece en el menú de inicio

El siguiente paso que voy a hacer ahora es:

Reiniciar el PC

Entrar en la BIOS

Activar virtualización (Intel VT-x / AMD-V)

Luego volver a Windows para continuar con:

Verificar virtualización

Instalar Ubuntu

Comprobar GPU NVIDIA en WSL

Instalar Axolotl correctamente

Quiero que me guíes paso a paso, sin saltar pasos, hasta dejar listo el entorno para fine-tuning con GPU.


nos quedamos en el paso 9 de gemini

wsl -l -v


